node_modules/
dist/
.astro/
.env
.env.*
.DS_Store
*.log
.vercel/
.netlify/
kilo.json
originales/
__pycache__/
*.pyc

# Config backups keep the same secrets the originals do (kilo.json is ignored
# above for exactly that reason). Rotation scripts leave .bak-<timestamp> files.
*.bak-*
